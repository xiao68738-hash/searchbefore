# v10 Play 候選版實機驗收

2026-09-26，SUGAR C60／Android 11。僅代表以下實測範圍通過，未放行正式軌道。

## 版本來源

- v10／1.1.4 在 13:11 發布至既有內部測試軌道，release 6。未動 Alpha、正式版或名單。
- 手機透過 Google Play「更新」從 v9 原地升級，非側載或重新安裝。
- 安裝後 `versionCode=10`、`versionName=1.1.4`、min24／target36；installer 為 `com.android.vending`。
- 首次安裝時間 9/17 07:45:22 保留；最新更新時間 9/26 13:15:11。
- 實際畫面顯示「版本 1.1.4」。此中性文字不代表已發布正式軌道。

## 登入、同步與資料

- 原有 Google 登入及同步同意保留，未登出、切換帳號、解除安裝或清除資料。
- 手動同步顯示伺服器同步成功，時間 `2026-09-26T05:19:39.412Z`。
- force-stop／重新開啟後仍登入、同步同意保留，同步時間仍存在。
- 更新後及同步重啟後分別透過 App 匯出完整 JSON。兩份 data 均與 v9 更新前基準（v9 同步重啟後）逐欄相同：`equal: true`、`changedFields: []`、exit 0。
- 2 個田區、1 筆用藥、5 筆農務、0 個配方、0 個最近作物。所有私人 JSON 僅存本機 private/native-v9-acceptance-20260926，不入 Git。v10 檔案 3902 bytes，外層版本字串由 1.1.4-internal 改為 1.1.4；不能以位元組不同誤判資料遺失。

## 公開功能抽測

- 不結球萵苣與半結球萵苣 → 作物用藥總覽 → 普拔克的露菌病入口可正常開啟，卡片為全寬；用法頁保留僅原登記、不合併分類提示。
- 未選用法的計算頁要求先從查詢帶入，沒有自行假設倍數。
- 選取用法後顯示鎖定 700 倍；水量 1 公升／1 桶顯示 1.428571 mL，與 1000÷700 的換算一致。這是 UI／數值路徑測試，不是用藥推薦，未儲存新施藥紀錄。
- 五張原始手機圖存本機 releases/native-v10-candidate-20260926/screenshots，均已逐張檢視、未含帳號或私人田區。部分畫面跨捲動區，僅供素材挑選與驗收，不宣稱已完成正式商店素材。未修圖、未上傳商店，沒有新增平板圖。

## 界限及剩餘工作

- 這不是從空白環境重新登入／全量還原測試；沒有重跑第二帳號隔離或刪除流程。不要把舊版測試冒稱 v10 本次測試。
- 空配方基準不能證明非空配方移轉；偏好不在 JSON 中。本次重啟是 App 程序重啟，不是手機重新開機。
- Play 發布檢查兩則警告：未提供去混淆檔、原生偵錯符號。未混淆版本無 mapping；不得捏造依賴套件符號。
- 使用者確認專用審查帳號尚未準備。Console 存取聲明、政策部署、資料安全最終核對、素材及品質結果仍須收尾；不能由本次實機通過直接推出「可安全正式發布」。
