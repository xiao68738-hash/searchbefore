# 噴前查 SearchBefore — 開發交接

寫給接手的人（含 AI 代理）。**先讀完「零、安全紅線」再動任何一行程式。**

更新日期：2026-08-01　｜　目前版本：`APP_VERSION 0.3.6.3`

> 本文件是**現況與地雷**的總覽。既有的 `docs/` 與 `mrl-data/資料夾索引.md` 仍是各領域的權威說明，本文不重複，只指路。

---

## 零、安全紅線（不可協商）

這是一個**農友拿去決定今天要不要噴藥**的工具。錯誤不會當場報錯，而是幾週後變成殘留超標或違規。

1. **任何殘留／合法性結論，在人工確認前不得進 App 或資料管線。** 名稱查無、分類不明、任一層對照失敗時，只能標「無法確認」，**不得推論安全或違規**。
2. **不得由作物分類／群組外推合法性。** `PQC_SAFETY.directCropLevels` 刻意只回傳作物本身、不併入上層群組（官方逐項校驗完成前）。任何地方想「順便展開群組」都要先想清楚——這等於替農友做未經查證的合法性判斷。
3. **作物名查無時，不可只憑名稱近似逕自等同。** 見下方「胡麻事件」。
4. **安全採收期只能取保守值。** 一律經 `PQC_SAFETY.effectivePhi`，取「`phi` 欄位」與「備註天數」的**較長者**。全庫有 330 列兩者不同，其中 235 列若用原始 `phi` 會顯示**過短**的採收期，並經由「帶入計算／記施藥」污染採收倒數。
5. **不得把未確認結果描述成安全、危險、高風險或保證合規。** 「安全採收期」是法規欄位名稱可以保留；但「保證安全／通過驗證／零風險／永久免費」等承諾不得使用。測試會擋部分高風險字句，人工複核仍不可省略。
6. 混合劑：**任一成分於該作物缺標準**才成立「不得檢出」。
7. **尚未完成、尚未驗收或尚未正式串接的功能，不得出現在正式前端。** 若只對指定測試者開放，入口、標題與主要操作都必須明確標示「開發中」；不可用「測試版／第一版」等模糊字樣代替。

---

## 一、環境與基本操作

| 項目 | 值 |
|---|---|
| Repo | `E:\dev\searchbefore`（**外接碟，會離線**；離線時所有操作都會失敗） |
| 平台 | Windows 10，Git Bash + PowerShell 皆可 |
| 部署 | **目前：Cloudflare → GitHub Pages `main` 根目錄 → https://searchbefore.tw**；Firebase `dist/` 白名單流程已備妥但尚未接管正式網域 |
| 遠端 | `xiao68738-hash/searchbefore`，主幹 `main`，**squash merge** |

```bash
cd "E:/dev/searchbefore" && node tests/run-all.js     # 提交前必跑
```

**RTK**：使用者環境裝了 `rtk`（token 壓縮代理），已設全域 PreToolUse hook 自動改寫 Bash 指令，不必手動加前綴。它只壓縮 shell 輸出，對瀏覽器／檔案讀取無效。

---

## 二、架構

**單一 `index.html`（約 6.0MB）**，其中 `const DATA=` 內嵌藥劑資料。另有正式頁面模組、功能旗標模組與尚未發布的純內部模組，必須清楚區分。

主要模組：

| 檔案 | 職責 |
|---|---|
| `safety.js` | `PQC_SAFETY`：採收期、劑型、作物層級。**安全核心** |
| `query-aids.js` | `PQC_AIDS`：害物從屬、種子處理辨識、**藥劑本位索引** |
| `farm-records.js` | `PQC_FARM`：田間紀錄時間軸、綜合表 |
| `export-formats.js` | `PQC_EXPORT`：xlsx／CSV／CRC32 |
| `crop-forms.js` | `PQC_CROP_FORMS`：收穫型態消歧 |
| `cloud-sync.js` | `PQC_SYNC`：Firestore 同步（攔在 `store.set` 單一出口） |
| `account.js` / `service-config.js` | 帳號、公開設定 |
| `form-ocr.js` / `form-ocr-ui.js` | OCR 草稿核心與介面；目前 `formOcr=development`，入口受測試驗證碼限制，使用 Cloud Run／Google Cloud Vision，完成部署與驗收後才能評估公開，結果仍須人工確認 |
| `web-support-config.js` | 各環境按需載入的綠界純自願支持設定；`googlePlayVoluntarySupport` 採全通路緊急關閉，避免 App 身分訊號晚到時誤顯示 |
| `tap-workflow.js` | **內部開發限定**：資訊服務專員協作狀態模型；不得放入 `index.html`、Service Worker 或正式 build |
| `dev/tap-workflow-review.html` / `dev/tap-workflow-review-ui.js` | **本機開發測試限定**：農友送審、專員退回、農友修訂、內部覆核及人工登打聲明流程；全程使用假資料、無網路傳輸與持久化，且不進入正式 build |
| `tap-activity-mapping.js` | **內部開發限定**：特殊作業與官方項目對照模型；保留原文、限制官方來源並要求人工確認，不得進入正式前端或 build |
| `dev/tap-activity-mapping.html` / `dev/tap-activity-mapping-ui.js` | **本機開發測試限定**：特殊作業人工對照流程與多筆待覆核清單試作；可依待確認、已確認、暫不對照篩選。只用 `DEMO-NOT-OFFICIAL` 假資料、無網路傳輸與持久化，且不進入正式 build |

> ⚠ **不要對 `index.html` 做無範圍 `grep`。** `DATA` 是單一巨行，會吐出數 MB。務必先 `grep -n` 定位再讀 ±15 行，或用 `awk 'length($0)<2000'` 過濾。（本次交接前我犯了兩次。）

---

## 三、開發流程（違反會出事，都出過事）

1. **一個變更 = 一個獨立 PR，一律從最新 `origin/main` 開分支。**
   絕不在已開／可能已被 squash 合併的分支上追加 commit——會產生**永遠進不了 main 的孤兒 commit**。已發生過四次（#74、#75、#77，以及 #76 把胡麻的安全校正弄丟）。
   追加前先確認：`git log origin/main --oneline -3`。
2. **預設直接開下一個 PR**，假設使用者自行合併；不要問「要不要合併」。
3. **改 `index.html` 必須換版**：
   ```bash
   node scripts/bump-version.mjs <下一版號> <slug>
   ```
   這支腳本一次改四處（`index.html` 的 `APP_VERSION`、`sw.js` 的 `CACHE_VERSION`、測試裡兩個釘住值）。**手動改容易漏**，漏了 `CACHE_VERSION` 會讓 PWA 續發舊快取——你電腦上看到更新了，農友那邊沒有，而且不會報錯。
   測試裡的版本 pin 要維持**跳脫點**格式（`/v0\.3\.1\.0-.../`），否則該腳本會拒絕執行。
4. 開新分支可用 `node scripts/new-branch.mjs`（或 `npm run branch`）。
5. **新增要進正式成品的 `.js` 模組**才做完整儀式：script 標籤 + `sw.js` PRECACHE + `index-syntax.test.js` 斷言 + `run-all.js` 註冊 + 換版。內部開發模組則相反：必須用測試確保它沒有被正式頁面、快取或 build 載入。
6. 提交前 `node tests/run-all.js` 全綠。以繁體中文溝通。

---

## 四、資料管線

- **App 的 DATA**：`scripts/build-data.mjs`（需先 `fetch-pesticide-source.mjs` 取 `mrl-data/source-cache/`，該快取已被清理，需重抓）。聚合規則：同「作物×病蟲害×成分×劑型×含量×用法」合併為一列，各家商品名收進 `bl` 陣列。
- **MRL 比對管線**：全部在 `mrl-data/`，**入口讀 `mrl-data/資料夾索引.md`**（七階段工作地圖）。管線只讀 `.json`，`.csv` 都是人工檢視用、可重新產生。

---

## 五、目前進度與未完成事項

### 主要待辦

| # | 事項 | 狀態 |
|---|---|---|
| 27 | **確認封閉測試並重產 .aab** | **只有擁有者能在 Play Console 確認**目前軌道、versionCode、14 天測試與 target API 狀態；網頁 `APP_VERSION` 不能用來推定 AAB 是否已送達。指南見 `docs/Android重新產生aab.md` |
| 3 / 4 / 5 | MRL 人工確認：殘留物定義 77 種、查無藥名 5 項、作物分類 44 種 | 需人工，見 `mrl-data/★先看我-校對說明.md` |
| 6 / 7 / 9 | MRL 功能實作（蒜三級標示 → 擴大豆類 → 全量開放） | 被 3/4/5 卡住 |
| 24 | FCM 推播提醒 | 上架後再評估 |
| 34 | 產銷履歷 L3 API 直傳 | **阻塞**；等待申請、規格、測試環境與補助認列書面答覆，見 `docs/產銷履歷串接-路線圖.md` |
| 35 | 資訊服務專員協作試辦 | **內部狀態模型與測試已完成第一版**；政府確認信、90 天試辦與公司成立決策已整理於 `docs/農業部溝通與公司成立決策.md`；尚未寄信，也尚未建立後端權限、分享同意、工作台或正式前端入口 |

### 「登記但不得檢出」複核（安全關鍵，未完成）

95 筆候選目前已全數建立複核紀錄：**檢核完成 50｜有疑義 15｜待確認／待簽核／待回覆 30｜未複核 0**。後端白名單目前只有 17 筆，前端仍未啟用。

- 只有「人工已確認＋檢核完成＋判定確認」可進後端白名單。
- 胡麻、群組成員與成分定義等仍有疑義者一律留待人工或主管機關確認。
- 可刪除的「誤判」只收「判定=誤判 **且** 狀態=檢核完成」者，其餘一律留著。**這道閘門不要放寬。**
- 完整數量與檔案位置以 `mrl-data/資料夾索引.md` 為準，不要在交接文件另行推算。

---

## 六、踩過的坑（**這節最值錢**）

### 1. 胡麻事件：不可擅自等同近似作物名
附表一**沒有「胡麻」**，卻同時有「**芝麻**」與「**亞麻籽**」兩個獨立項、容許量不同。台灣農藥慣以胡麻稱芝麻（Sesamum indicum），但中文亦可指亞麻籽（Linum）。

我曾逕自當成芝麻、把三筆改判「誤判／可刪」——那等於**刪掉真正該保留的不得檢出警告**。經使用者指正後全部退回「存疑」。

> **通則**：作物名查無時，先確認**登記證的真正作物身分**，再決定套哪一列。只有「同一作物不同寫法」（薏苡＝薏仁、葉用甘藷＝小葉菜類、檬果＝芒果）且無競爭項時才可等同。

### 2. 孤兒 commit
PR 被 squash 合併後又往同分支推 → 那些 commit 永遠進不了 main。**推之前先確認 PR 是否已合併。**

### 3. 深色模式：寫死 `#fff` 的卡片
新增元件若背景寫死白色而沒加 `html[data-theme="dark"]` 覆寫，深色下會變成淺字白底（實測對比 1.26:1，完全讀不到）。新增卡片類元件時，記得加進既有的 `background:var(--surface)` 覆寫群組。

### 4. `getComputedStyle` 在預覽面板的假象
用 JS 事後 `setAttribute("data-theme","dark")` 後，**靜態節點的 computed style 不會更新**（連 inline style 都不反映），會讓你以為 CSS 沒生效。正確做法：用 App 自己的主題偏好（`localStorage.displayPrefs.theme="dark"`）後**重新載入整份文件**再量。

### 5. 驗證資料時比對鍵要完整
比對藥劑列時 `agentKey` 含 `phi`。我漏了 `phi` 就出現 176 筆假差異，追下去才發現是自己配對錯。**先懷疑自己的比對方法，再懷疑資料。**

### 6. Service Worker 快取
本機驗證改版時，若沒清 SW 快取會一直看到舊頁面。清法：`getRegistrations().unregister()` + `caches.delete()` 後重新載入。

---

## 七、對外往來現況

| 對象 | 狀態 |
|---|---|
| **防檢署** | **已回覆結案**（115/7/28 防檢三字第1151842628號）：允諾補正開放資料的英文名稱欄位。同時更正我方兩處錯誤，詳見 `mrl-data/詢問函-防檢署-回覆.txt`。**待其補正上線後應重抓快照、以官方值取代 `人工查證英文名.json`（待辦 #4）** |
| **食藥署** | 已寄，待回覆 |
| **農糧署／凌網（產銷履歷 TAP）** | 已確認 SOAP+XML、申請時提供一位已驗證 L3 經營者測試、完成介接後其他 L3 經營者不需逐一重申請、規格與測試環境須先通過申請、代碼由 API 取得，原則上所需 API 都要提出申請。下一步須寄出確認信，書面確認人員正式職稱、計酬認列、個別驗證者可否作為第一位測試對象，以及個人開發者是否須具公司／商業登記；信稿與決策點見 `docs/農業部溝通與公司成立決策.md`。 |
| **綠界金流** | 純自願支持連結已完成；Google Play 僅因「款項給創作者且不提供任何數位回饋」的 P2P contribution 例外開放，並保留遠端關閉開關。未來 App 內數位功能仍只能走 Google Play Billing。 |

---

## 八、給接手者的建議順序

1. **先跑 `node tests/run-all.js`**，確認環境可用。
2. 讀 `mrl-data/資料夾索引.md`（MRL 全貌）與本文件第六節（坑）。
3. 使用者回饋點5、點6已由 **PR #89** 完成；點7也已改為「尚無法確認」的中性文案。MRL 對照資料仍未開放，後續不要把文案完成誤認成資料已可上線。
4. **不要**在 MRL 人工確認（#3/#4/#5）完成前碰 #6/#7/#9——那些都建立在尚未查證的對照上。
5. 有疑義時**保守**：寧可標「無法確認」，不要猜。這個 App 的價值就是農友信得過它；被誤導一次，之後所有提示都會被忽略。
6. 產銷履歷先推進 #35 的訪談、試辦與內部狀態模型；#34 在取得正式規格前不得自訂代碼、假造成功狀態或做出看似可上傳的按鈕。
7. `tap-workflow.js` 目前只是一個純狀態模型，不是權限系統。多人協作前仍要完成後端工作區權限、農民明示分享同意、事件稽核與隱私政策更新。
