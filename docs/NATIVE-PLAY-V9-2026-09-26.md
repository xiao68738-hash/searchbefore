# v9 Play 實機驗收

日期：2026-09-26（臺北）。這是單一實機的有界驗收，不是完整正式版放行。

## 已完成

- 使用 SUGAR C60／Android 11，確認 Wi-Fi 已連線且 Android 判定網路可用。
- 透過手機 Google Play 的「更新」，由 v8／1.1.3-internal 原地升級至 v9／1.1.4-internal。未側載、卸載、清除資料或切換帳號。
- installer 為 `com.android.vending`；首次安裝時間仍為 9/17 07:45:22；更新時間為 9/26 12:46:47。min24／target36。
- 原有 Google 登入及同步同意保留。畫面顯示 2 個田區、1 筆用藥、5 筆農務、0 個配方。
- 更新前、更新後，以及手動同步後 force-stop／重新開啟後，均透過 App 的完整 JSON 匯出流程取樣。
- `scripts/compare-native-backups.cjs` 比較兩份後測與更新前基準，皆 `equal: true`、`changedFields: []`、exit 0。比較完整 data，僅忽略外層版本／匯出時間，不只是筆數。
- 手動同步顯示「已完成與伺服器的紀錄同步」，完整同步時間為 `2026-09-26T04:51:16.495Z`；重啟後此時間、登入及同步同意保留。
- 查詢抽測：不結球萵苣與半結球萵苣 → 作物用藥總覽。全寬卡片及防治對象按鈕正常；普拔克 → 露菌病可開啟此作物 × 此對象的原登記用法。

## 證據保管

三份 JSON 僅存於本機 private/native-v9-acceptance-20260926，不提交 Git、不公開帳號或紀錄內容。公開查詢畫面存於本機 audits/native-v9-20260922/v9-overview-play-20260926.png，帶有真實內部測試標示，不作正式版截圖。

## 未涵蓋

- 本次沒有登出／重新登入、切換第二帳號、清空後雲端還原、刪除雲端資料或新增真實紀錄。先前版本的驗收不得冒稱本次重跑。
- 配方與最近作物基準皆為空，不能據此證明非空配方移轉；顯示偏好不在此 JSON 範圍。
- 同步成功及既有資料完整，不等於本次驗證了全新裝置還原或所有服務端安全規則。
- v10 候選與商店登入申報、公開政策、最終素材等另行放行。v9 成功不自動代表 v10 通過。
