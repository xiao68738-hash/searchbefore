# 噴前查交接文件

更新日期：2026-07-14  
目前安全修正版本：`v1.3.0`（分支 `fix/safety-update`）

## 專案概況

噴前查是給台灣農友使用的 PWA，功能包括農藥登記查詢、稀釋計算、施藥紀錄及安全採收期倒數。

- GitHub：https://github.com/xiao68738-hash/searchbefore
- 正式網站：https://xiao68738-hash.github.io/searchbefore/
- 農藥資料來源：農業部農藥開放資料

## 目前檔案

| 檔案 | 用途 |
|---|---|
| `index.html` | 主要畫面、農藥資料與應用程式功能 |
| `safety.js` | 可獨立測試的安全計算核心 |
| `tests/safety.test.js` | 不需額外套件的安全測試 harness |
| `sw.js` | PWA 離線快取 |
| `manifest.webmanifest` | PWA 設定 |
| `privacy.html` | 隱私權政策 |
| `.gitignore` | 防止私鑰與暫存檔上傳 |

## 不可破壞的安全規則

1. 粉劑以公克 `g` 顯示，絕不可換算成瓶蓋、茶匙或湯匙。
2. 只有液劑 `ml` 可以顯示容量估算，實際配製仍以量具與標示為準。
3. 採收備註若列出露天、設施或不同用途天數，在沒有栽培方式選項前採最長天數。
4. 同一田區或種植批次的每一筆施藥都必須納入計算，取最晚可採日期。
5. 任一筆施藥紀錄缺少採收期時，該批次不得顯示「可採收」。
6. `track`、`notify` 或畫面篩選只能控制提醒與顯示，不能把實際施藥排除於安全計算。
7. 作物群組 `PARENT` 尚未逐項完成官方校驗，目前不可自動合併上層群組登記。

## 本次 v1.3.0 修正

- 加入首頁醒目安全提醒。
- 粉劑移除瓶蓋與湯匙換算。
- 新增備註採收期解析，設施栽培採較長天數。
- 原始 `phi` 缺值但備註有採收天數時，可用保守天數建立紀錄。
- 採收期完全缺值時仍可保存施藥紀錄，但不顯示可採日，也不建立通知。
- 倒數改為依田區/種植批次彙總所有施藥紀錄。
- 舊手機通知按單筆施藥排程，可能早於同田區後續施藥的最晚日期，因此 v1.3.0 暫停自動通知；使用者可從安全彙總後的倒數卡加入行事曆。
- 加入紀錄 ID、`SCHEMA_VERSION`、舊資料備份及遷移。
- 暫停未完成校驗的作物群組聯集。
- 加入 Node.js 安全測試與發布前檢查。
- 更新隱私權政策，說明 Google 表單與電子郵件回報。

## 執行測試

```text
node tests/run-all.js
```

所有測試通過後，才可以合併到 `main`。

## GitHub 保存與發布流程

1. `main` 永遠保留可公開使用的穩定版。
2. 從 `main` 建立一個工作分支，例如 `fix/safety-update`。
3. 在工作分支完成修改與測試。
4. 保存成有意義的 Commit。
5. 建立草稿 Pull Request，檢查差異及測試結果。
6. 確認後合併到 `main`，GitHub Pages 才會更新正式網站。
7. 網站確認正常後，刪除已完成的工作分支並建立版本標籤。

## 發布檢查

- `APP_VERSION` 與發布版本一致。
- `sw.js` 的 `CACHE_VERSION` 已更新。
- `safety.js` 已列入 Service Worker 快取。
- 安全測試全部通過。
- 手機與桌面瀏覽器可完成查詢、計算、紀錄及倒數。
- 離線重新開啟仍能使用。
- `.pem` 私鑰沒有出現在 GitHub 差異中。

## 授權私鑰

- `private_key.pem` 只能保存在開發者控制的加密備份中，絕不可上傳 GitHub、雲端公開連結或通訊軟體。
- `.gitignore` 已排除 `*.pem`，但發布前仍須人工檢查。
- 建議記錄公鑰指紋、建立日期與輪替方式；私鑰疑似外流時必須更換金鑰對及 App 內公鑰。
- 授權碼產生器若不含私鑰或其他祕密，可以版本化；加入 GitHub 前先人工檢查內容。

## 後續資料模型方向

未來採收紀錄應屬於「田區／種植批次」，不是單一噴藥紀錄。建議逐步分為：

- `cropBatch`：田區、作物、種植日期與批次。
- `applicationRecord`：每次實際施藥。
- `harvestEvent`：採收日期與數量。

提醒功能可以讓農友選擇是否通知，但所有 `applicationRecord` 都必須影響該批次的最晚可採日。
