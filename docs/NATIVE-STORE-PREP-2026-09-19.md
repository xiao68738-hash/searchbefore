# 原生版上架準備：2026-09-19

## 本輪確認

- 既有來源 `0c52061` 的 GitHub Review 及 Native Android CI 均成功（35366381949／35366381820）。不是把前一提交的結果當成本輪結果。
- 手機仍為 Google Play v8／1.1.3-internal，首次安裝時間 9/17 07:45:22，最後更新 9/18 23:57:18。沒有重裝或清除資料。
- 擷取 7 張 720×1640 原始畫面，僅公開查詢與計算；原圖與雜湊清冊在本機 `D:/SearchBefore/releases/native-store-captures-20260919/`。未上傳商店。

## 擷取時發現的問題與修正

「作物用藥總覽」使用未指定寬度的 Card，短藥名會縮成窄卡，留下大塊空白。改用與其他畫面一致的全寬 BrandCard；原登記防治對象逐一顯示為至少 48dp 高的全寬按鈕，保留長字換行。

總覽步驟標題改為「作物用藥總覽」，補充依名稱彙整不代表不同劑型、含量或採收部位可以互用。未修改 catalog、分類、同步、帳號或備份格式。夜蛾類與甜菜夜蛾仍是兩個獨立點選目標；重複相同目標只列一次。

新增 300dp／1.5 倍文字 UI 回歸，檢查卡片全寬、點擊高度、重複去除與兩個回呼分開。畫面測試結果於完成後補記，不因已寫測試而宣稱通過。

## 已完成的本機驗證

- `build-android-native.ps1 -Lint -UiTestApk`：5 分 49 秒成功；71 JVM tests、0 failures、0 errors；Lint `No issues found`。
- 網站 `tests/run-all.js` 全部通過；web → Kotlin → web 完整合成備份比對通過（2 田區、2 用藥、6 類農務、2 配方）。
- Play Console 9/19 本輪唯讀核對：v8 全面提供內部測試、Alpha 仍為 v4。沒有建立或發布新版本。
- 啟動環境問題：Windows PowerShell 5 的 Start-Process 遇到既有 Path／PATH 重複環境鍵；改用既有 PowerShell 7 執行同一腳本後成功啟動獨立只讀模擬器。未修改全域 PATH，也未操作手機系統設定。
- UI 執行目標限定 `emulator-5580`、API36、360×640／160dpi、1.5 倍字體。完整 **24/24 通過，148.137 秒**，含新增的總覽測試；紀錄目錄 `D:/SearchBefore/audits/native-ui-20260919-092023/`。未跳過或放寬失敗斷言，未將 debug 模擬器通過宣稱為新的 Play 正式包驗收。
- 測試後用一般字體實際操作「蔥 → 作物用藥總覽」，原始畫面 `overview-fixed-top.png`／`overview-fixed-cards.png` 已目視確認：標題正確、卡片滿寬、原登記入口可讀、頁首與導覽未被覆蓋。這是獨立模擬器預覽，不混入實機商店候選圖。完成後正常關閉本輪模擬器。

## 素材限制

實機畫面仍有「內部測試版」，不能修圖移除後冒充正式版。原圖中的總覽窄卡列為問題證據，不能選為最終素材；正式候選版與平板畫面仍須重拍。未包含 Google 帳號、田間紀錄或開發中的 OCR。

## 正式發布仍保留的關卡

1. 專用審查帳號及 Play 存取聲明；不提供使用者私人帳號密碼、不要求審查者自建帳號。
2. OAuth 品牌與正式版標示、最終簽章 AAB、正式候選實機回歸。
3. 正式手機／平板素材、商店文字與隱私／刪除說明一致性。
4. Google 預先發布報告及可用的 vitals 證據；無資料不等於零問題。

手機 v8 與 Play Alpha／正式版不因本機程式修改而更新。沒有修改 Firebase 規則或新增測試者。
