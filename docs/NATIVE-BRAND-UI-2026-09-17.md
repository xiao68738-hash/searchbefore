# 原生品牌介面與正式版接續驗收

> 9/18 更新：新版介面已隨 v6 發布內部測試，且真實手機 Play v5→v6 原地升級、既有登入／完整資料／同步與程序重啟驗收通過。以下是前一輪實作紀錄；最新發布證據見 NATIVE-PLAY-V6-2026-09-18.md。沒有發布 Alpha／正式版。

## 範圍

使用者確認 v5 外觀尚未完成，要求繼續正式版工作。本輪只更新原生 UI／驗收，不發布或替換 Alpha／正式版，也不覆蓋手機的 Play v5。使用既有透明品牌圖，不生成新的 LOGO、不加入外部字型或套件。

## 已實作

- `NativeTheme.kt`：米白背景、白色卡片、深綠標題、橘色選取狀態；字級、行高、圓角及邊框統一。
- 品牌頁首與程式繪製的五個線條圖示。仍標示內部／開發版本，沒有拿掉未驗收標記。
- 作物／病蟲害以可點擊卡片呈現；相關分類仍只在底部導引，不合併登記。
- 用法和採收期獨立區塊：內容寬度低於 320dp 或系統字體大於 1.25 倍時上下排列，避免擠在兩欄。
- 重要附註、特殊用途、不適用型態與殘留提醒保留；未知採收期不顯示為 0 天。沒有稀釋倍數的用法不新增計算器。
- 登記用量保留原數值／單位，明確區別面積施用量與每桶配藥量；不任意換算成家庭用量。
- 個人頁把資料、登入／同步、備份放前面；通知設定在後。長狀態訊息可捲動，鍵盤顯示時預留空間。
- 個人頁新增固定公開網址的隱私政策／帳號刪除申請入口；不附 UID／token，不自動寄信或刪除資料。隱私政策補原生本機儲存、手動同步與匯出保管說明（政策仍在候選分支）。

## 驗證

- `build-android-native.ps1 -Lint`：BUILD SUCCESSFUL（15m38s），55 項 JVM 測試 0 failures／0 errors，Lint `No issues found.`，debug APK 完成。
- Firebase 設定的 13 項合成選取／拒絕檢查通過；沒有在輸出或 Git 放入設定 JSON／私鑰。
- 主要前景／背景對比計算：一般字 13.30、次要字 6.45、綠色按鈕白字 6.38、警語 8.96、橘色選取 6.43。不是全頁無障礙驗收的替代。
- 真實模擬器 720×1280／320dpi／1.5 倍字體的首頁已目視確認可讀；公開畫面存於本機 audits，不含私人帳號／田間紀錄。
- 新增兩項合成 UI 測試：300dp／1.5 倍字體下警語與操作可捲動；種子處理不出現假倍數／計算鈕。擴充原本查詢導覽測試的公開首頁截圖。
- Android instrumentation 首輪 10 項中 9 項通過；查詢鍵盤焦點與結果捲動修正後，10 項全通過。加入隱私入口後重新建置 4m55s，55 JVM／Lint 通過，Android 16 最新 11 項全通過（50.474s）。重跑前在測試外重設獨立模擬器通知權限，避免既有授權讓權限測試前提失效；不操作手機通知權限。
- 真實 Play v5 JSON 副本含 2 田區／1 用藥／4 農務／0 配方。以編譯後原生 Backup 類別、網站 readBackup/buildBackup 往返，數值正規化後一致、來源紀錄欄位遺失數 0。`scripts/NativeBackupRoundTrip.java` 為不覆寫輸入／輸出的離線驗收工具，不記錄私人欄位值，不操作 APP 或雲端。
- 網站 #147 已合併並部署；#149 欄位白名單相容修正亦已合併／部署成功。另補新建紀錄才有的登記編號／收穫型態回歸；私人副本沒有配方，不能據此宣稱真實配方移轉已驗收。
- 9/18 00:07–00:10，Play v5 正式套件新增 `TEST_ONLY_PLAY_V5_SYNC_20260918` 設備紀錄（作業明確寫 TEST_SYNC_NOT_REAL_FARM_OPERATION；不建施藥／採收），上傳完成時間 2026-09-17T16:07:28.740Z。既有預覽套件更新至新 UI，更新前有 2／1／4、沒有新標記；同本人帳號手動同步後 2／1／5，標記恰好 1 筆，時間 16:09:56.909Z。獨立套件確實收到新紀錄，不只是上傳端顯示成功。兩套件在同一手機，接收端不是空白資料，因此不宣稱第二台全新裝置完整還原。
- 16:11:45.897Z 接收端重複同步後仍 2／1／5、標記 1 筆。已恢復預覽版同步關閉與 Gboard 注音；正式 v5 的手動同步同意不變。新品牌首頁已在 Android11 手機目視確認，截圖在本機 audits，沒有私人紀錄。測試設備紀錄仍保留，未刪除。
- 接續準備 versionCode 6／1.1.1-internal；JSON 匯出版本改取 BuildConfig，不再將正式內部包寫成舊 preview 版本。僅建內部候選，不授權 Alpha 或正式發布。

## 環境問題如實保留

本輪 Gradle 建置和 Android16 模擬器並行時，模擬器曾出現 `System UI isn't responding`，不是測試通過。縮小獨立模擬器解析度、待建置負載降低後首頁才正常；後續操作測試另記。沒有關閉或清除使用者手機程式來掩蓋問題。

## 尚未完成的正式版工作

1. 下一個 Play 內部候選包與實機檢查；11 項模擬器回歸通過不等於全頁無障礙或 Play v5 已更新。
2. 全新空白裝置還原及使用者真實完整配方／偏好與 UI 匯入驗收。正式 v5 新增上傳→獨立套件下載與非破壞性格式往返已通過，不將它冒充全流程操作驗收。
3. 查詢群組延伸、全部頁面無障礙等尚待界定／驗收的相容性差異，見 android-native/README.md。
4. 通知、離線、跨帳號与發布候選的完整回歸。既有 Play v5 登入證據見 NATIVE-PLAY-PHONE-2026-09-17.md，不等於全功能正式通過。

本輪沒有改動 Firestore 規則、Google 權限、同步協定、原登記資料、套件簽章或既有使用者紀錄。新增 1 筆明確標記的設備同步測試，保留供對照；正式 Play v5 未被旁載取代，僅更新獨立預覽套件並保留它的資料。
