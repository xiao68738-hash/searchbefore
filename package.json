{
  "name": "searchbefore",
  "version": "0.2.0",
  "private": true,
  "description": "噴前查正式發布建置與測試工具",
  "scripts": {
    "test": "node tests/run-all.js",
    "build": "node scripts/build-release.mjs",
    "release:check": "npm run test && npm run build && node scripts/check-release.mjs",
    "firebase:login": "npx --yes firebase-tools@15.24.0 login",
    "security:headers": "node scripts/check-security-headers.mjs",
    "data:check-pesticides": "node scripts/check-pesticide-update.mjs",
    "deploy:preview": "npm run release:check && npx --yes firebase-tools@15.24.0 hosting:channel:deploy preview",
    "deploy:hosting": "npm run release:check && npx --yes firebase-tools@15.24.0 deploy --only hosting",
    "release:bump": "node scripts/bump-version.mjs",
    "branch": "node scripts/new-branch.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.43.1"
  },
  "dependencies": {
    "@paddleocr/paddleocr-js": "^0.4.2",
    "pinyin-pro": "^3.18.2"
  }
}
